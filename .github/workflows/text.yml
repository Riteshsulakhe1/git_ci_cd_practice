name: CI
on: [push, pull_request]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
      with:
        jestArgs: src/*.test.js
    - name: Run tests
      run: npm test
      